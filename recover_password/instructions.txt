In the page Recover_Password, we don't have a field to put a username
--> Inspect : username is webmaster@born2sec.com --> max_len == 15
Doesn't work
So curl, with a short username : 
curl -X POST "http://192.168.1.16/?page=recover" \
  -d "mail=test&Submit=Submit"

the responses contains the flag